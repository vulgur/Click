package com.pku.ss.egl.libs;

import com.pku.ss.egl.et.*;
// basic library

library BootControllerLib

    private bc BootComputer{};

    function sleep()
        bc.sleep();
    end

    function shutDown()
        bc.shutDown();
    end

    function lock()
        bc.lock();
    end

    function logOff()
        bc.logOff();
    end

    function hibernate()
        bc.hibernate();
    end
    
    function reboot()
    	bc.reboot();
    end

end
